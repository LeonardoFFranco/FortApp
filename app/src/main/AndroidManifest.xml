<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upb.fortapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activites.LoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activites.MainActivity"/>
        <activity android:name=".ui.activites.MenuActivity"/>
        <activity android:name=".ui.activites.MapActivity"/>
        <activity android:name=".ui.activites.BattlePassActivity"/>
        <activity android:name=".ui.activites.ItemShopActivity"/>
        <activity android:name=".ui.activites.WeaponsDetailsActivity"/>
        <activity android:name=".ui.activites.WeaponsListActivity"/>
        <activity android:name=".ui.activites.Weapons2Activity"/>
        <activity android:name=".ui.activites.WeaponsDetails2"/>
        <activity android:name=".ui.activites.NoticiasActivity"/>


    </application>
</manifest>